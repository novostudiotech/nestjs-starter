# =============================================================================
# Test Environment Configuration
# =============================================================================
# This file is used by Playwright E2E tests
# Values are configured for local testing with Docker Compose
# =============================================================================

# Application Port (tests will use this)
PORT=13000

# Log Level (reduce noise in tests)
LOG_LEVEL=error

# CORS Origin (allow all for tests)
CORS_ORIGINS=true

# =============================================================================
# Test Database Configuration
# =============================================================================

# Test Database URL
# This database is cleaned between test runs
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nestjs_foundation_test

# =============================================================================
# Authentication Configuration
# =============================================================================

# Auth Secret (test value - NOT for production)
# This is a test-only secret, safe to commit
AUTH_SECRET=test-secret-key-for-e2e-tests-only-do-not-use-in-production-min-32-chars

# =============================================================================
# Email Configuration (Resend)
# =============================================================================

# Resend API Key (optional for tests)
# Tests can mock email sending
RESEND_API_KEY=

# Sender Email Address (optional for tests)
EMAIL_FROM=

# Reply-To Email Address (optional for tests)
EMAIL_REPLY_TO=

# Application Name (optional for tests)
APP_NAME="NestJS Starter Boilerplate"

# =============================================================================
# Error Monitoring (Disabled in tests)
# =============================================================================

# Sentry DSN (leave empty to disable in tests)
SENTRY_DSN=

# Sentry Environment
SENTRY_ENVIRONMENT=test
